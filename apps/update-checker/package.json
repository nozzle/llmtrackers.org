{
  "name": "update-checker",
  "private": true,
  "type": "module",
  "dependencies": {
    "@llm-tracker/github": "workspace:*",
    "@llm-tracker/shared": "workspace:*"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "backfill-review-sites": "tsx src/backfill-review-sites.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250306.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.71.0"
  }
}
