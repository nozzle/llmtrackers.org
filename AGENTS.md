# Repository Guidelines

- Keep changes focused and follow existing project conventions.
- Before creating a commit, run `pnpm check` and resolve any failures.
- Do not rely on git hooks; run required checks manually.
- In your handoff, note any checks you could not run.
